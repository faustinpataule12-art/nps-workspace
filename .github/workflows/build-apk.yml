name: Build APK

on: 
   push:
      branches: [ main ]
   workflow_dispatch:

iobs:
   build:
     runs-on: ubuntu-latest
    steps:
     - uses: actions/checkout@v4
     - name: Set up JAVA
       uses: actions/setup-java@v4
        with:
          distribution: 'temur',
          java-version: '17'
